{
  "name": "finalsys",
  "version": "1.0.0",
  "description": "Financial Analysis System",
  "main": "start.js",
  "scripts": {
    "start": "node start.js",
    "build": "pip install --upgrade pip && pip install -r requirements.txt",
    "vercel-build": "echo 'Build step completed'",
    "postinstall": "pip install --upgrade pip && pip install -r requirements.txt"
  },
  "dependencies": {
    "child_process": "^1.0.2"
  },
  "engines": {
    "node": ">=14.0.0",
    "python": "3.9"
  }
}
